@page "/tasks/{boardId:Guid}/task/{id:Guid}/edit"

@using Tasker.Web.Pages.Tasks.Components.SaveTask;
@model Tasker.Web.Pages.Tasks.Edit

@{
  ViewData["Title"] = "Tarefa";
}

@(await Component.InvokeAsync("SaveTask", new { boardId = Model.BoardId, model = Model, id = Model.Id }))

